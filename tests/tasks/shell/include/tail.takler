date
wait                      # wait for background process to stop
python -m takler.client complete --host ${TAKLER_HOST} --port ${TAKLER_PORT} \
      --node-path ${TAKLER_NAME}
trap 0                    # Remove all traps
exit 0                    # End the shell
